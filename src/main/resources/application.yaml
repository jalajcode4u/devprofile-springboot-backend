spring:
  application:
    name: devprofile

  datasource:
    url: jdbc:mysql://localhost:3306/devprofile
    username: root
    password: root123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080
